# 11 — Risk register

| ID | Risk | Evidence base | Mitigation (designed-in) | Kill switch |
|---|---|---|---|---|
| R1 | **Fixture-milestone theater** — declaring cache/MTP/serving "done" on stubs; substance done twice | Helios M06–M11 all shipped on fixtures; P-track redid them | Real-tensor rule in 10; gates name tensor-level evidence explicitly | Reviewer refuses acceptance without ledger rows |
| R2 | **MTP marathon** — chasing default-on for months below gate | Helios XR14→XR86: +19.97% < 25%, ~80 goal contracts | MTP is post-V1; any reopened lane has an explicit 2-round cap, and parking is a valid outcome | 2 sub-gate A/B rounds → park + decision record |
| R3 | **Parked-experiment sink** (runtime SSD, cache codecs, exotic kernels) | Helios P07 disabled; XR09 q4 breaks greedy, 0.000% active relief; XR13 18/18 fail | P2 streamed conversion is required; Qwen KV compression is conditional P6; runtime SSD and custom formats require a named workload/payoff gate | Any optional lane exhausts its predeclared trial budget without MEASURED payoff → decision record + park |
| R4 | **Execution-model drift** — convenience `concatenate`/deferred eval creeping back | Helios tails: p99 511–2162 ms vs p50 82 ms; 16K peak 21.87 GB | Shared invariants in 03; family-specific transaction tests; tail gate after the relevant fast-path/kernel promotion; protected release/measurement perf canary ±5% | Canary alarm blocks promotion |
| R5 | **Serving-layer theater** — faux streaming, Python-on-request-path, backend forks | Helios 3-frame SSE, per-request `mlx_lm` subprocess tokenization | Single native backend; in-process tokenizer; real SSE contract tests | Contract tests are tier-1 CI |
| R6 | **Template/tool wire-format drift** — community templates get thinking/tool interleave wrong; silent quality loss | HF discussion #115 (31B template bugs); Helios shipped two wrong Gemma chat formatters | Exact-checkpoint conversation profiles; frozen golden fixtures vs pinned reference rendering; render/parser CI | Fixture mismatch fails tier-1 |
| R7 | **Governor constants ported not derived** (Qwen ×6 fudge on Gemma geometry) | bonsai ADR 0005 ×6 single-sample fudge; measurement-gates.md cross-model warning | Constants derived per architecture/deployment/operation plan, calibrated per machine, and re-derived after relevant kernel/cache changes | Calibration error band breach = G4 fail |
| R8 | **Strict-JSON gap** — greedy-only + post-hoc repair masking arg corruption | bonsai wellformed 93.06% was server-repaired upper bound; expected-tool share 26.39% | Profile-vocabulary constrained-JSON candidate + repair telemetry honesty split + validator-disposes downstream | Constrained arm must dominate or remain separately scoped/opt-in |
| R9 | **Low-N/single-sample promotion** | bonsai ×6 from one miss; Helios 16K/32K low_n rows; "32K" was a 1-token probe | Trial minimums; candidate-min > baseline-max; low_n rows cannot gate | Ledger schema enforces labels |
| R10 | **Vendor-fact drift** — MLX SDPA dispatch and family implementations move under us | MLX 0.32.0→0.32.1 changed SDPA dispatch; Qwen P2 still needs exact oracle pins | Pin MLX production control and each family oracle independently; any upgrade is its own A/B'd decision, never ambient | Any pin bump without decision record reverts |
| R11 | **Exactness overclaim** — gating/advertising "MTP-on == MTP-off byte-identical," which is unachievable at fp16/bf16 scale | NunSpark plan5-m2: ~1 near-tie argmax flip/110 tokens from verify-pass shape; their bit-identity gate failed on real workloads, auto-disable-on-divergence is self-defeating | Three-tier G1 (08): same-shape bit-exact / kernel fault-threshold / **cross-shape target-verified**; `near_tie_events` is required before any spec lane | An unexplained divergence (no logged near-tie) fails the gate as a real bug |
| R12 | **Trim rollback on a rotated sliding-window ring** — silent KV corruption in long agentic sessions | NunSpark's own gemma4-MTP path uses `kv.truncate` = acknowledged latent bug (their backlog #4); `RotatingKVCache` not trimmable post-rotation | Append-only rollback (discard-by-not-committing); trim-or-drop module modeled on `PrefixCache`, shared by any Gemma MTP/cache lane (A2, 05/07) | Any reopened lane proves rollback after forced rotation before promotion |
| R13 | **Governor sizes to the ceiling, not the optimum** — resident set past the cliff collapses throughput | NunSpark MEASURED 16GB M4 sweep 4/5/6/8/10 GB → 1.79/2.52/**3.23**/2.84/1.33 tok/s; macOS compressor contention; live-availability clamp reverted | Governor targets the measured throughput optimum below OOM; each family/deployment A-arm locates it; size off total RAM, never live availability (A4, 05) | Throughput-vs-budget curve regression in the deployment's governor probe |
| R14 | **Speculating on a MoE tier** — spec decode can be slower than greedy on MoE | NunSpark MEASURED spec-slower-than-greedy across 30B/120B/235B/480B (expert-union I/O grows with γ, acceptance doesn't) | Any post-V1 MoE lane defaults to greedy + expert caching; speculation remains off absent its own positive A/B | Any MoE spec A/B below greedy → spec stays off, recorded |
| R15 | **Family-semantic leakage** — Qwen tensors/state/template interpreted by Gemma code or vice versa | Qwen3.8 is recurrent+GQA with untied head and different tool/thinking grammar; current native graph/parser are Gemma-shaped | Strict top-level dispatch before tokenizer/native; closed architecture and checkpoint conversation profiles; conflict tests; recognition != support | Any cross-family fallback or optional-field coercion blocks merge |
| R16 | **Partial recurrent-state publication** — failed/cancelled Qwen work corrupts the next token/session | GDN state mutates every step and cannot be repaired by trimming ordinary KV | Stage recurrent+KV updates; commit after successful eval; abort/replay/cancel tests; state identity in snapshots | Non-replayable state or unexplained step/chunk drift fails P3 |
| R17 | **Conversion/artifact ambiguity** — huge source, omitted components, sanitation renames, or partial output yields a plausible wrong model | Qwen source is ~55.56 GB with text, vision, and MTP tensors; local disk is constrained | Bounded resumable conversion; explicit consumed/omitted inventory; canonical names; per-file/root hashes; atomic manifest publication; hostile-input tests | Missing/extra/duplicate tensor, hash drift, or incomplete marker rejects load |
| R18 | **Two-bit fit succeeds but behavior fails** — file size passes while reasoning/tools/recurrent stability collapse | Sub-3-bit quality is tensor- and task-sensitive; 16 GB target needs aggressive compression | BF16/Q3 controls, mixed scalar sensitivity, protected recurrent metadata, paired free-running/long-state eval, measured live peak | Candidate misses any predeclared per-task/state/memory floor → do not promote |

Standing posture risks (recorded, not engineering items): Gemma 4 Prohibited-Use-Policy
ambiguity (04 §License; affects redistribution posture only); A12 identity-tuple discipline —
Hyperion evidence is local/dev tier and production tuples requalify on their own substrate;
solo-founder calendar
— the active P-phase dependency DAG is the prioritization, and no implemented slice is accepted
without its declared evidence.
