# 11 — Risk register

| ID | Risk | Evidence base | Mitigation (designed-in) | Kill switch |
|---|---|---|---|---|
| R1 | **Fixture-milestone theater** — declaring cache/MTP/serving "done" on stubs; substance done twice | Helios M06–M11 all shipped on fixtures; P-track redid them | Real-tensor rule in 10; gates name tensor-level evidence explicitly | Reviewer refuses acceptance without ledger rows |
| R2 | **MTP marathon** — chasing default-on for months below gate | Helios XR14→XR86: +19.97% < 25%, ~80 goal contracts | MTP sequenced M7 after fast dense path; explicit 2-round cap; opt-in park is a success outcome | 2 sub-gate A/B rounds → park + decision record |
| R3 | **Parked-experiment sink** (SSD tier, KV compression, exotic kernels) | Helios P07 disabled; XR09 q4 breaks greedy, 0.000% active relief; XR13 18/18 fail | Both excluded from v1; post-v1 lanes require a real-workload payoff hypothesis first | Any lane 2 weeks w/o MEASURED payoff → decision record + park |
| R4 | **Execution-model drift** — convenience `concatenate`/deferred eval creeping back | Helios tails: p99 511–2162 ms vs p50 82 ms; 16K peak 21.87 GB | Ruled model in 03 (§1–5); CI tail gate p99 ≤ 2×p50 post-M4; nightly perf canary ±5% | Canary alarm blocks merge |
| R5 | **Serving-layer theater** — faux streaming, Python-on-request-path, backend forks | Helios 3-frame SSE, per-request `mlx_lm` subprocess tokenization | Single native backend; in-process tokenizer; real SSE contract tests | Contract tests are tier-1 CI |
| R6 | **Template/tool wire-format drift** — community templates get thinking/tool interleave wrong; silent quality loss | HF discussion #115 (31B template bugs); Helios shipped two wrong Gemma chat formatters | Golden fixtures vs transformers `apply_chat_template` (pinned); fixtures frozen; render CI | Fixture mismatch fails tier-1 |
| R7 | **Governor constants ported not derived** (Qwen ×6 fudge on Gemma geometry) | bonsai ADR 0005 ×6 single-sample fudge; measurement-gates.md cross-model warning | Constants derived from Geometry, calibrated per-machine; re-derived after M4 kernels; both calibrations in ledger | Calibration error band breach = G4 fail |
| R8 | **Strict-JSON gap** — greedy-only + post-hoc repair masking arg corruption | bonsai wellformed 93.06% was server-repaired upper bound; expected-tool share 26.39% | Constrained-JSON lane (M5) + repair telemetry honesty split + validator-disposes downstream | Constrained arm must dominate or ship opt-in (recorded) |
| R9 | **Low-N/single-sample promotion** | bonsai ×6 from one miss; Helios 16K/32K low_n rows; "32K" was a 1-token probe | Trial minimums; candidate-min > baseline-max; low_n rows cannot gate | Ledger schema enforces labels |
| R10 | **Vendor-fact drift** — MLX SDPA head-dim sets, NAX dispatch, mlx-lm Gemma fixes move under us | MLX 0.32.0→0.32.1 already changed SDPA dispatch; NAX op coverage undocumented; WWDC26 session 232 unpublished at research time | Pins everywhere (MLX 0.32.0, mlx-lm 0.31.3+); references/mlx-m5-facts.md carries an UNCONFIRMED ledger; upgrade = its own A/B'd decision record, never ambient | Any pin bump without decision record reverts |
| R11 | **Exactness overclaim** — gating/advertising "MTP-on == MTP-off byte-identical," which is unachievable at fp16/bf16 scale | NunSpark plan5-m2: ~1 near-tie argmax flip/110 tokens from verify-pass shape; their bit-identity gate failed on real workloads, auto-disable-on-divergence is self-defeating | Three-tier G1 (08): same-shape bit-exact / kernel fault-threshold / **cross-shape target-verified** + `near_tie_events` counter from M2 (A1) | An unexplained divergence (no logged near-tie) fails the gate as a real bug |
| R12 | **Trim rollback on a rotated sliding-window ring** — silent KV corruption in long agentic sessions | NunSpark's own gemma4-MTP path uses `kv.truncate` = acknowledged latent bug (their backlog #4); `RotatingKVCache` not trimmable post-rotation | Append-only rollback (discard-by-not-committing); trim-or-drop module modeled on `PrefixCache`, shared by MTP + conversation cache (A2, 05/07) | M7 gate requires rollback correctness proven **after a forced ring rotation** |
| R13 | **Governor sizes to the ceiling, not the optimum** — resident set past the cliff collapses throughput | NunSpark MEASURED 16GB M4 sweep 4/5/6/8/10 GB → 1.79/2.52/**3.23**/2.84/1.33 tok/s; macOS compressor contention; live-availability clamp reverted | Governor targets the *measured* throughput optimum (below the OOM ceiling); M1 budget sweep locates it; size off total RAM, never live availability (A4, 05) | Throughput-vs-budget curve regression in the nightly governor probe |
| R14 | **Speculating on the MoE tier** (M9) — spec decode is *slower* than greedy on MoE | NunSpark MEASURED spec-slower-than-greedy across 30B/120B/235B/480B (expert-union I/O grows with γ, acceptance doesn't) | M9 runs greedy + expert-caching, **spec OFF by default**; spec is the dense lever only (A5, 07/M9) | Any MoE spec A/B below greedy → spec stays off, recorded |

Standing posture risks (recorded, not engineering items): Gemma 4 Prohibited-Use-Policy
ambiguity (04 §License; affects O-2 only); A12 identity-tuple discipline — hyperion evidence
is local/dev tier, production tuples requalify on their own substrate; solo-founder calendar
— the milestone order IS the prioritization, and M0–M4 alone already deliver a usable fast
engine (M5–M8 are the agentic differentiation).
